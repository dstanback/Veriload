{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npm run build",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/notifications/daily-summary",
      "schedule": "0 8 * * 1-5"
    }
  ]
}
